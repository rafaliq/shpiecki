<section>
  <div class="hero">
    <div class="container">
      <h1 class="hero__title">
        <span class="headline">
          {{ $data['title'] }}
        </span>
      </h1>
    </div>
    <img src="{{ $data['bg']['url'] }}" alt="{{ $date['bg']['title'] }}" class="hero__bg">
  </div>
</section>